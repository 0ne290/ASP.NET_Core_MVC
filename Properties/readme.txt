environmentVariables.ASPNETCORE_ENVIRONMENT по умолчанию был установлен в Development. Логикой программы определено кастомное значение DatabaseSetup,
которое если установить, то в случае перезапуска сервера, будет доступен только один адрес /Admin/SetUserAndPasswordForDb/{user}/{password} и только
для главных админов. Если по нему перейти, то в конфигурационный файл appsettings.json будут занесены введенные в URL логин-пароль для входа в БД в
зашифрованном виде. Можно обойтись и ручным занесением логина-пароля в зашифрованном виде (все главные админы знают алгоритм шифрования, а также
имеют специальный скрипт для автоматического шифрования (как в первом случае, но сервер не нужен)) в файл appsettings.json, тогда сервер
потребуется перезапускать один раз вместо двух.

UPD: это все кнч хорошо, но я решил сделать только через скрипт - так проще